# Walkthrough: Rename to evohome_rs and Publish (Issue #2)

1. **Renamed Package**: Renamed `rust/Cargo.toml` package from `mytcc_rs` to `evohome_rs`.
2. **Updated References**: Replaced `mytcc_rs` with `evohome_rs` in all source files (`src/lib.rs`, `examples/*.rs`, `tests/*.rs`, `Cargo.toml`, `pyproject.toml`).
3. **Verified Tests**: Ran `cargo test` successfully (all tests passed).
4. **Committed Changes**: Committed the rename and documentation updates.
5. **Published**: Successfully published `evohome_rs` v0.1.1 to crates.io.
