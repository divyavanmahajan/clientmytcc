# Task: Bump Version to 0.2.0

As a developer, I want to bump the package version to `0.2.0` in the rust folder to prepare for the next release.

## Requirements
- Update `rust/Cargo.toml` to version `0.2.0`.
- Update `rust/pyproject.toml` to version `0.2.0`.
- Commit the changes.
- Push the changes to the remote repository.
